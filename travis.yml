language: java
jdk:
  - oraclejdk8
# command to run tests
script:
 - coverage run -m finalProject_sstTinker_constants finalProject_sttTinker_testRunner
after_success:
 - bash <(curl -s https://codecov.io/bash)
